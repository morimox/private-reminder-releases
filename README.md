# PrivateReminder Update Feed

The stable branch contains only update metadata. Windows packages are
immutable GitHub Release assets. This repository never contains schedules,
page contents, cookies, OAuth credentials, API keys, or other user data.
